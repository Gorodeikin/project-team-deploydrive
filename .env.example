# Public origin of the maintained backend API (no trailing slash).
# This is not a secret and is safe to expose to the browser (NEXT_PUBLIC_*).
# Do not commit a real .env.local file — it is git-ignored on purpose.
NEXT_PUBLIC_API_URL=https://deploydrive-api.onrender.com
